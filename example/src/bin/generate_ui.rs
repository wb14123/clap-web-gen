use example::generate_process_ui; // Auto-generated by #[web_ui_bind] macro
use std::fs;

fn main() {
    // The macro automatically generates generate_process_ui() function
    let html = generate_process_ui("example", "WASM Process Function");

    fs::write("generated_ui.html", html).expect("Failed to write HTML file");

    println!("✓ Generated UI written to 'generated_ui.html'");
    println!("\nTo use:");
    println!("  1. Build WASM: wasm-pack build --target web");
    println!("  2. Open generated_ui.html in a browser");
}
